<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.jiangzizheng.waterloocarpool">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <!-- To upload image in accountInfo -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".frontend.activities.MainActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".frontend.activities.GoogleLoginActivity"
                android:label="@string/title_activity_login">
        </activity>
        <activity
                android:name=".frontend.activities.EmailLoginActivity"
                android:label="@string/title_activity_email_login">
        </activity>
        <activity
                android:name=".frontend.activities.LoginActivity"
                android:label="@string/title_activity_login">
        </activity>
        <activity
                android:name=".frontend.activities.RegisterActivity"
                android:label="@string/title_activity_register">
        </activity>
        <activity
                android:name=".frontend.activities.AccountInfoActivity"
                android:label="@string/title_account_information">
        </activity>
        <activity
                android:name=".frontend.activities.TripDetailsActivity"
                android:label="@string/title_trip_details">
        </activity>
        <activity
                android:name=".frontend.activities.TripsList"
                android:label="@string/title_trips_list">
        </activity>
        <activity android:name=".frontend.activities.MyTripsActivity">
        </activity>
    </application>

</manifest>